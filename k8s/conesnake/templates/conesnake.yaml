{{- define "conesnake_common" -}}
imagePullPolicy: Always
envFrom:
- secretRef:
    name: "{{ .Release.Name }}-conesnake-secrets"
env:
- name: RUST_BACKTRACE
  value: "1"
ports:
- containerPort: 8080
  name: listen-port
startupProbe:
  httpGet:
    path: /
    port: listen-port
  failureThreshold: 15
  periodSeconds: 10
readinessProbe:
  httpGet:
    path: /ping
    port: listen-port
  failureThreshold: 2
  periodSeconds: 9
livenessProbe:
  httpGet:
    path: /
    port: 8080
  failureThreshold: 1
  periodSeconds: 5
{{- end -}}

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ .Release.Name }}-relay"
  labels:
    {{- include "common_labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "common_labels" . | nindent 6 }}
      app: relay
  replicas: 2
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  revisionHistoryLimit: 2
  template:
    metadata:
      annotations:
        rollme: {{ randAlphaNum 5 | quote }}
      labels:
        {{- include "common_labels" . | nindent 8 }}
        app: relay
    spec:
      serviceAccountName: "{{ .Release.Name }}-sa-get-pods"
      nodeSelector:
        mode: relay
      imagePullSecrets:
      - name: regcred
      terminationGracePeriodSeconds: 2
      containers:
      - name: "{{ .Release.Name }}-relay"
        {{- include "conesnake_common" . | nindent 8 }}
        image: {{ include "amd64_image" . }}
        resources:
          requests:
            memory: {{ .Values.relay.mem }}
        args:
        - "--port=8080"
        - "--max-width={{ .Values.max_width }}"
        - "--max-height={{ .Values.max_height }}"
        - "--max-snakes={{ .Values.max_snakes }}"
        - "--max-boards=0"
        - "--num-relay-reqs={{ $.Values.relay.num_reqs }}"
        - "--latency={{ .Values.relay.latency }}"
        - "--worker-latency={{ .Values.worker.latency }}"
        {{- range $worker := .Values.workers }}
        - "--worker=http://{{ $worker.service_ip }}:8080,{{ $worker.parallel}}"
        {{- end }}
        - "relay"

{{ range $worker := .Values.workers }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ $.Release.Name }}-worker-{{ $worker.hostname }}"
  labels:
    {{- include "common_labels" $ | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "common_labels" $ | nindent 6 }}
      app: "worker"
      node: "worker-{{ $worker.hostname }}"
  replicas: {{ $worker.replicas }}
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  revisionHistoryLimit: 2
  template:
    metadata:
      annotations:
        rollme: {{ randAlphaNum 5 | quote }}
      labels:
        {{- include "common_labels" $ | nindent 8 }}
        app: "worker"
        node: "worker-{{ $worker.hostname }}"
    spec:
      serviceAccountName: "{{ $.Release.Name }}-sa-get-pods"
      nodeName: "{{ $worker.hostname }}"
      imagePullSecrets:
      - name: regcred
      terminationGracePeriodSeconds: 2
      containers:
      - name: "{{ $.Release.Name }}-worker"
        {{- include "conesnake_common" $ | nindent 8 }}
        image: {{ include "amd64_image" $ }}
        resources:
          requests:
            memory: {{ $.Values.worker.mem }}
        args:
        - "--port=8080"
        - "--max-width={{ $.Values.max_width }}"
        - "--max-height={{ $.Values.max_height }}"
        - "--max-snakes={{ $.Values.max_snakes }}"
        - "--max-boards={{ $.Values.worker.boards }}"
        - "--latency={{ $.Values.worker.latency }}"
        - "--num-worker-threads={{ $.Values.worker.num_threads }}"
        - "worker"
{{ end }}
